import { Metadata } from "next";
import React from "react";

export const metadata: Metadata = {
  title: {
    default: "Next.js Complete",
    template: " | ",
  },
};

const Blog = () => {
  return <div>Blog</div>;
};

export default Blog;
